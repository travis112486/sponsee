{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "prev=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^1}\"; git cat-file -e \"$prev^{commit}\" 2>/dev/null || exit 1; git diff --quiet \"$prev\" HEAD -- apps/web packages/shared package.json pnpm-lock.yaml pnpm-workspace.yaml turbo.json vercel.json || exit 1; exit 0",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://sponsee.onrender.com/api/$1",
      "transforms": [
        {
          "type": "request.headers",
          "op": "set",
          "target": { "key": "x-sponsee-front-door" },
          "args": "$FRONT_DOOR_SECRET",
          "env": ["FRONT_DOOR_SECRET"]
        }
      ]
    },
    { "handle": "filesystem" },
    { "src": "/((?!assets/).*)", "dest": "/index.html" }
  ]
}
