{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "//include": "`api` holds the Vercel functions. They were outside the project until SPO-88, so nothing typechecked the waitlist endpoint that returns captured PII.",
  "include": ["src", "api"],
  "//exclude": "Vercel deploys this app with Root Directory `.`, so the build runs `pnpm install` against apps/marketing/package.json alone — no repo root, no root node_modules. vitest lives at the root, so typechecking a *.test.ts here made `tsc -b` fail on Vercel with TS2307 while passing locally and in CI, where resolution walks up to the root. Shipping vitest into the production build to fix that would be backwards; the tests are typechecked by tsconfig.test.json instead, which `pnpm typecheck` runs.",
  "exclude": ["**/*.test.ts", "**/*.test.tsx"]
}
